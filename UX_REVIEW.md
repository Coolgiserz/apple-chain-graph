# 前端交互与界面语言评审（产品经理 / 交互设计师视角）

> 分支：`feat/ux-review-pm-ixd`（基于 `main`）
> 方法：代码静态走查 + i18n 键交叉校验（`src/engine/*`、`templates/graph_page.html`、`locales/*`、`data/apple_supply_chain.json`）。**未做可用性测试 / 真机验证**，结论需在真实用户场景复核。
> 评审对象：当前版本（渐进式展开 + 沿边流动粒子 + 关键洞察浮层）。

---

## 〇、总体判断

方向是对的。上一版"默认铺开全部 115 个节点 → 毛线团 + 静止"的体验问题已被有效解决：
默认只看「产品线 → 产品 → 零部件」三层、供应商按需展开，信息密度大幅下降；流动粒子让图谱"活"了起来，首屏"关键洞察"卡把静态空间变成了可读结论。

但**在"交互语义的一致性"和"动效的克制"上还有明显可改进点**，其中几处是会让用户困惑、甚至与默认态自相矛盾的硬伤。下面按优先级列出。

---

## 一、交互逻辑问题（按优先级）

### P0-1 「重置视图」与默认态自相矛盾 ⚠️ 最该先改
`interaction.js` 的 reset 处理会 `cbP=cbC=cbS=true`，即重置后**勾选了"全部供应商"**，等于把视角推回"全展开"。但我们这一版刻意把默认态设成"供应商隐藏、按需展开"。

后果：用户精心停留在的渐进式默认视图，点一下"重置"反而跑到了最拥挤的全展开态——既违背"重置=回到默认"，也削弱了这次重构的核心价值。

建议：reset 应恢复到**当前设计的默认态**（cbS 保持不勾、expanded 清空、line/q 清空），而不是"显示全部"。

### P0-2 「全部供应商」复选框语义混淆：总开关 vs 逐项展开
该复选框现在身兼两职：
- 勾选 = 显示**所有**供应商；
- 取消勾选 = 收起**所有**已逐项展开的节点（丢弃用户的心血）。

用户心智里"展开某些节点看细节"和"一次性看全部"是两件事，却被一个会互相覆盖的开关绑死。

建议：把"展开全部 / 收起全部"拆成独立的**动作按钮**，与"点击单节点展开"解耦。复选框（或开关）只表达"是否默认显示供应商层"，不应在切换时抹掉用户的逐项展开状态。

### P0-3 单击即"展开/收起"：点一下就误收起
当前 `onNodeClick` 对产品/零部件是**切换**展开态。用户若只是想再点一下看面板信息，却会把已展开的供应商收起。

建议：分离"选中看信息"与"展开结构"——
- 单击节点 = 选中并打开信息面板（不收起）；
- 展开/收起改由节点上的「+」徽标、或面板内的"展开供应商"按钮触发；
- 或约定：单击选中，双击展开。

### P0-4 流动动效不尊重 `prefers-reduced-motion`
`S.flow` 默认 `true`，且 `loop()` 在粒子开启时**永不停止 rAF**。对前庭敏感用户、低功耗设备、以及"想要安静读图"的场景都不友好。

建议：
- 启动时读取系统 `prefers-reduced-motion: reduce`，若用户要求减少动效则默认 `flow=false`；
- 即便 flow 开，静止且无交互一阵后应允许循环降级（当前只有 flow 关才停），降低常驻 CPU。

---

## 二、交互细节（中优先级）

### P1-5 默认粒子密度偏高，可能"活"得过头
默认视图含 产品 + 零部件（约 396 条 `USES` 边），全部同时流动，画面偏忙，反而可能抵消"活"的正面感受，甚至显得噪。

建议：默认只做**极轻的全局脉动**，或**仅在选中/悬停的子图上流动**；粒子更小、更慢、更淡。把"满屏流动"作为可选项而非默认全开。

### P1-6 产品线节点是"聚合"却被画成实体圆点
`Line` 节点用与普通实体相同的圆形（`BASE_R.Line` 只是更大），仅靠紫色区分。用户很可能误以为它是供应链里真实的一环（某个厂商/某个零件），而非"分类聚合"。

建议：用**形状/描边**区分"聚合/分类"与"实体"——例如聚合节点用圆角矩形或虚线环、无填充光晕；实体节点保持实心圆。仅靠颜色不足以传达"这是类别"。

### P1-7 搜索展开供应商后状态不一致
搜索会跨全部节点匹配（含隐藏供应商）并临时显示，但全局"供应商隐藏"态不变；清空搜索后供应商又消失（除非勾了全部）。且搜索结果数量只在右侧"企业表格"里体现，画布附近无反馈。

建议：搜索期间在搜索框旁显示**结果计数**；搜索命中的节点在清空搜索前保持可见，避免"闪一下就没了"。

### P1-8 缺常驻图例
节点配色（蓝=产品 / 琥珀=零部件 / 绿=供应商 / 紫=产品线）与风险色（红=高 / ⚠=单点依赖）在常规模式无任何说明——模板里 `#legend` 恒为 `display:none`，只在风险视图语义下才有意义，但普通模式从不出现。

建议：提供常驻（或首屏一次性）图例，至少说明四类节点颜色与"单点依赖"标记。

---

## 三、界面语言 / 文案（术语一致性）

**正向**：`field.*` 标签本地化到位（发布时间 / 起售价(USD) / 代工 / 脆弱性 / 最弱环节…），节点中英双语命名清晰（如「Apple 自研 SoC 芯片 / Apple Silicon SoC」），`product_line` 枚举（音频 / Mac / 空间计算 / 可穿戴 / iPad / iPhone）准确。i18n 键经交叉校验**零缺失**，多语言骨架健康。

待改进：
- **P1「全部供应商」**：作为复选框文案与"渐进式展开"心智冲突，读起来像过滤器，实则是"展开全部"动作。建议改为「展开全部供应商」并优先考虑用按钮而非复选框（呼应 P0-2）。
- **P1 首屏提示过长**：hint 是一整句"滚轮缩放 · 拖拽画布平移 · 拖动节点 · 单击产品/零部件展开其供应商 · 流动粒子表示供应链流向"，信息密度高、首屏易被忽略。
  建议分层：顶栏常驻极简提示（拖拽/缩放），首次进入用**一次性引导卡**点明"点产品/零部件展开其供应商"，而非把所有规则塞进一行。
- **P1「关键洞察」内容偏描述、欠建议**：当前是"规模=计数、最脆弱产品线=名称"。建议升级为**可操作结论**，如"iPhone 线有 N 个单点依赖，建议优先关注 X（可点击聚焦）"，让浮层从"展示"走向"建议"。
- **P2 顶栏控件密集**：企业表格 / 风险视图 / 重置 / 适配 / 关键洞察 / 流动 六七个控件无图标、无分组；移动端易拥挤。

---

## 四、可发现性与首屏体验

- 默认供应商隐藏后，新用户可能误以为"数据缺失 / 没加载出来"。建议首屏用一行轻提示或洞察卡明确："供应商默认隐藏，点击产品/零部件展开"。
- 「关键洞察」卡首屏自动弹出占左下角，与图形可能重叠，部分用户会当成干扰。建议：保留首屏弹出，但提供"不再提示"或收起为更紧凑形态。

---

## 五、响应式 / 性能

- 顶栏 6+ 控件在窄屏可能换行拥挤（hint 已移动端隐藏，但按钮未做处理）。建议真机验证，移动端将次要控件收进「⋯」菜单。
- 115 节点 + 521 边 + 常驻 rAF，低端机可能掉帧。建议：粒子按设备/视口降采样；当前已仅对可见边绘制，方向正确，可进一步在移动端降低粒子数与频率。

---

## 六、落地优先级清单（供后续 feat 参考）

| 优先级 | 条目 | 类型 |
|---|---|---|
| P0 | 重置回到默认态（供应商隐藏） | 逻辑修复 |
| P0 | 拆分"展开全部/收起全部"动作与逐项展开 | 交互/控件 |
| P0 | 分离"选中看信息"与"展开结构"（避免误收起） | 交互 |
| P0 | 尊重 `prefers-reduced-motion` + 静止降级 | 无障碍/性能 |
| P1 | 默认粒子密度收敛（仅子图流动 / 更轻） | 动效克制 |
| P1 | 产品线聚合节点用形状/描边区分实体 | 视觉语义 |
| P1 | 常驻图例（节点色 + 单点标记） | 可理解性 |
| P1 | 搜索结果计数 + 清空前保持可见 | 一致性 |
| P1 | 文案：「全部供应商」→ 按钮化；首屏引导卡替代长 hint | 文案/引导 |
| P1 | 「关键洞察」从描述升级为可操作建议 | 信息价值 |
| P2 | 移动端控件收进「⋯」菜单；粒子按设备降采样 | 响应式/性能 |

---

## 七、评审者注记

- 本评审基于**代码与文案静态走查**，未做眼动/可用性测试，结论（尤其 P1 的密度、引导措辞）需在真实用户样本上验证后再拍板。
- 所有判断均有代码依据（已逐条对应 `src/engine/*`、`templates/graph_page.html`、`locales/*`）；i18n 完整性已用脚本交叉校验，确认无缺失键，故未将其列为问题。
