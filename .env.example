# Docker 构建用的镜像源（仅本地生效，不会提交到仓库）
#
# 国内 / 网络受限环境：把基础镜像指向 docker.io 的镜像源（用完整镜像名）。
# 例如华为云 SWR 的 ddn-k8s 镜像（已验证可拉取）：
PYTHON_IMG=swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/python:3.11-slim
NGINX_IMG=swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/nginx:1.27-alpine
#
# 海外 / 可直连 docker.io 的环境：整段注释掉即可（compose 会用默认官方源）。
# PYTHON_IMG=python:3.11-slim
# NGINX_IMG=nginx:1.27-alpine
